{
  "hash": "692579a617c2d66b9883688fe431cb75",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"Dendrogram\"\ndescription: \"A hierarchical clustering drawn straight from hclust(), cut into coloured groups.\"\ncategories: [Hierarchies]\n---\n\n\n\n`vdendrogram()` draws a base `hclust` (or `dendrogram`) as a node-link tree:\nleaves along one edge, each merge at its height, joined by right-angle brackets.\nIt coerces the clustering to `vgraph()`'s node and edge tables, so the result is\nan ordinary plot spec you can theme and render like any other.\n\nPass `k` to cut the tree with `cutree()` and colour the branches and leaf labels\nby cluster. Branches above the cut, where the clusters join, stay grey.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nhc <- hclust(dist(scale(mtcars)), method = \"ward.D2\")\np <- vdendrogram(hc, k = 4)\n```\n:::\n\n\n\n::: {.cell}\n::: {.cell-output-display}\n![](figs/dendrogram-cut.png){width=100%}\n:::\n:::\n\n\nWith 32 leaves the labels are tight along the bottom. `direction = \"right\"`\ngrows the tree sideways and lays the labels out horizontally, which reads better\nfor a long list of leaves.\n\n\n::: {.cell}\n\n```{.r .cell-code}\np <- vdendrogram(hc, k = 4, direction = \"right\")\n```\n:::\n\n\n\n::: {.cell}\n::: {.cell-output-display}\n![](figs/dendrogram-right.png){width=100%}\n:::\n:::\n\n\nFor finer control, skip the preset. `vgraph()` takes the clustering directly, and\n`layout = \"dendrogram\"` reads the merge heights; you then add the marks yourself.\n`layout = \"unrooted\"` (from `graphlayouts`) instead spreads the tree across the\nplane the way phylogenies are drawn.\n",
    "supporting": [],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}